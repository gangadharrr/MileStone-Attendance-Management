<script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
<script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
<script>
    function myDeleteDegree(link, Message) {
        let text = "Are you sure you want to delete Degree " + Message + " ?";
        let _link = link;
        if (confirm(text) == true) {

            window.open(link, '_self');
        }
    } function myDeleteBranch(link, Message) {
        let text = "Are you sure you want to delete Branch " + Message + " ?";
        let _link = link;
        if (confirm(text) == true) {

            window.open(link, '_self');
        }
    }function myDeleteCourse(link, Message) {
        let text = "Are you sure you want to delete Course " + Message + " ?";
        let _link = link;
        if (confirm(text) == true) {

            window.open(link, '_self');
        }
    }
    function myDeleteAll(link, Message) {
        let text = "Are you sure you want to delete All " + Message + " ?";
        let _link = link;
        if (confirm(text) == true) {

            window.open(link, '_self');
        }
    }
    function myDeleteCoursesAssigned(link, Message) {
        let text = "Are you sure you want to delete Course Assigned " + Message + " ?";
        let _link = link;
        if (confirm(text) == true) {

            window.open(link, '_self');
        }
    }
        function myDeleteSectionsAssigned(link, Message) {
        let text = "Are you sure you want to delete Section Assigned" + Message + " ?";
        let _link = link;
        if (confirm(text) == true) {

            window.open(link, '_self');
        }
    }function myDeleteStudent(link, Message) {
        let text = "Are you sure you want to delete Student " + Message + " ?";
        let _link = link;
        if (confirm(text) == true) {

            window.open(link, '_self');
        }
    }function myDeleteEmployee(link, Message) {
        let text = "Are you sure you want to delete Employee " + Message + " ?";
        let _link = link;
        if (confirm(text) == true) {

            window.open(link, '_self');
        }
    }
</script>
